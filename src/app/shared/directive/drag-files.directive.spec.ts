import { DragFilesDirective } from './drag-files.directive';

describe('DragFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new DragFilesDirective();
    expect(directive).toBeTruthy();
  });
});
